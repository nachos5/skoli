<!DOCTYPE html>
<html lang="is">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
    crossorigin="anonymous">
  <link href="https://fonts.googleapis.com/css?family=Open+Sans|Source+Sans+Pro:700" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <title>
    <%= title %>
  </title>
</head>

<body>
  <header>

    <nav>
      <div class="d-flex">
        <!--  navbar-light bg-light -->
        <ul class="col-8 nav nav-tabs">
          <li class="nav-item">
            <a class="nav-link <%= (path === locals.urls.apply) ? 'active' : ''  %>" href="<%= locals.urls.apply %>">Atvinnuumsókn</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= (path === locals.urls.login) ? 'active' : ''  %><%= (!locals.user) ? '' : 'd-none' %>"
              href="<%= locals.urls.login %>">Innskráning</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= (path === locals.urls.register) ? 'active' : ''  %>" href="<%= locals.urls.register %>">Nýskráning</a>
          </li>
          <% if (locals.user) { %>
          <li class="nav-item">
            <a class="nav-link <%= (path === locals.urls.applications) ? 'active' : ''  %>" href="<%= locals.urls.applications %>">Atvinnuumsóknir</a>
          </li>
          <% } %>
        </ul>
        <ul class="col-4 nav nav-tabs d-flex flow-row justify-content-end">
          <li class="nav-item <%= (locals.user) ? '' : 'd-none' %>">
            <a class="nav-link <%= (path === locals.urls.admin) ? 'active' : ''  %>" href="<%= locals.urls.admin %>">Stjórnborð</a>
          </li>
          <li class="nav-item">
            <form class="nav-link <%= (locals.user) ? '' : 'd-none' %>" action="<%= locals.urls.logout %>" method="post">
              <input id="logout-button" class="" type="submit" value="Útskrá">
            </form>
          </li>
        </ul>
      </div>
    </nav>
    
  </header>
  <main>
